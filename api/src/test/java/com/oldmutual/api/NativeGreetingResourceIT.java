package com.oldmutual.api;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends GreetingRoutesTest {

    // Execute the same tests but in native mode.
}